{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "npm@9.6.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "tsup": "^6.7.0",
    "turbo": "^2.5.0"
  }
}
